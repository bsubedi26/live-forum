{
  "name": "live-forum-main",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "start": "npm run client && npm run server",
    "client": "cd client && npm start",
    "server": "cd server && npm start"
  }
}
