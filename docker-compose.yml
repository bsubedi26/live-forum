services:
  app:
    build: .
    ports:
      - "${PORT:-3030}:3030"
    environment:
      NODE_ENV: production
      PORT: "3030"
    volumes:
      - forum-data:/app/data
    restart: unless-stopped

volumes:
  forum-data:
